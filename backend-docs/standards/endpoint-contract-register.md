# Endpoint Contract Register

This table standardizes contract documentation per endpoint and acts as the canonical checklist for publishing API docs.

| Endpoint | Methods | Auth Requirements | Request Contract Source | Response Contract Source | Status Expectations | Error Expectations | Example Coverage |
|---|---|---|---|---|---|---|---|
| `/afo-register` | `on_get` | `public/guest` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/afo-register/suggestions` | `on_get` | `public/guest` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/afo-register/texts-numbers` | `on_post` | `public/guest` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/bibliography` | `on_get, on_post` | `write:bibliography` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/bibliography/all` | `on_get` | `public/guest` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/bibliography/list` | `on_get` | `public/guest` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/bibliography/{id_}` | `on_get, on_post` | `write:bibliography` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/cached-markup` | `unknown` | `public/guest` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/corpus/all-signs` | `unknown` | `public/guest` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/corpus/chapters/all` | `on_get` | `public/guest` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/corpus/query` | `on_get` | `public/guest` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/corpus/texts/all` | `on_get` | `public/guest` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/dossiers` | `on_get` | `public/guest` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/dossiers/filter` | `on_get` | `public/guest` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/dossiers/search` | `on_get` | `public/guest` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/dossiers/suggestions` | `on_get` | `public/guest` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/findspots` | `on_get` | `public/guest` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/folios/{name}/{number}` | `on_get` | `dynamic:folio-read` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/fragments` | `on_get` | `public/guest` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/fragments/all` | `on_get` | `public/guest` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/fragments/all-ocred-signs` | `unknown` | `public/guest` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/fragments/all-signs` | `unknown` | `public/guest` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/fragments/colophon-names` | `on_get` | `public/guest` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/fragments/latest` | `unknown` | `public/guest` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/fragments/query` | `on_get` | `public/guest` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/fragments/query-by-traditional-references` | `on_post` | `public/guest` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/fragments/retrieve-all` | `on_get` | `public/guest` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/fragments/{number}` | `on_get` | `dynamic:fragment-read` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/fragments/{number}/annotations` | `on_get, on_post` | `annotate:fragments` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/fragments/{number}/archaeology` | `on_post` | `transliterate:fragments` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/fragments/{number}/collect-lemmas` | `on_get` | `lemmatize:fragments` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/fragments/{number}/colophon` | `on_post` | `transliterate:fragments` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/fragments/{number}/corpus` | `on_get` | `public/guest` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/fragments/{number}/date` | `on_post` | `transliterate:fragments` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/fragments/{number}/dates-in-text` | `on_post` | `transliterate:fragments` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/fragments/{number}/edition` | `on_post` | `transliterate:fragments` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/fragments/{number}/genres` | `on_post` | `transliterate:fragments` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/fragments/{number}/lemma-annotation` | `on_post` | `lemmatize:fragments` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/fragments/{number}/lemmatization` | `on_post` | `lemmatize:fragments` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/fragments/{number}/match` | `on_get` | `transliterate:fragments` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/fragments/{number}/named-entities` | `on_get, on_post` | `transliterate:fragments` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/fragments/{number}/pager` | `unknown` | `public/guest` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/fragments/{number}/pager/{folio_name}/{folio_number}` | `on_get` | `dynamic:folio-read` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/fragments/{number}/photo` | `on_get` | `dynamic:fragment-read` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/fragments/{number}/references` | `on_post` | `transliterate:fragments` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/fragments/{number}/scopes` | `on_post` | `dynamic:fragment-read` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/fragments/{number}/script` | `on_post` | `transliterate:fragments` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/fragments/{number}/thumbnail/{resolution}` | `on_get` | `dynamic:fragment-read` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/genres` | `on_get` | `public/guest` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/images/{file_name}` | `on_get` | `public/guest` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/lemmas` | `on_get` | `public/guest` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/lemmasearch` | `on_get` | `public/guest` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/markup` | `unknown` | `public/guest` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/periods` | `on_get` | `public/guest` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/provenances` | `on_get` | `public/guest` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/signs` | `on_get` | `public/guest` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/signs/all` | `on_get` | `public/guest` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/signs/transliteration/{line}` | `on_get` | `public/guest` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/signs/{sign_name}` | `on_get` | `public/guest` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/signs/{sign_name}/images` | `on_get` | `public/guest` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/signs/{sign_name}/{sort_era}` | `on_get` | `public/guest` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/statistics` | `unknown` | `public/guest` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/texts` | `on_get` | `public/guest` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/texts/{genre}/{category}/{index}` | `on_get` | `public/guest` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/texts/{genre}/{category}/{index}/chapters/{stage}/{name}` | `on_get` | `public/guest` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/texts/{genre}/{category}/{index}/chapters/{stage}/{name}/alignment` | `on_post` | `write:texts` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/texts/{genre}/{category}/{index}/chapters/{stage}/{name}/colophons` | `on_get` | `public/guest` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/texts/{genre}/{category}/{index}/chapters/{stage}/{name}/display` | `on_get` | `public/guest` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/texts/{genre}/{category}/{index}/chapters/{stage}/{name}/extant_lines` | `on_get` | `public/guest` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/texts/{genre}/{category}/{index}/chapters/{stage}/{name}/import` | `on_post` | `write:texts` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/texts/{genre}/{category}/{index}/chapters/{stage}/{name}/lemmatization` | `on_post` | `lemmatize:fragments` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/texts/{genre}/{category}/{index}/chapters/{stage}/{name}/lines` | `on_post` | `write:texts` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/texts/{genre}/{category}/{index}/chapters/{stage}/{name}/lines/{{number}}` | `on_get` | `public/guest` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/texts/{genre}/{category}/{index}/chapters/{stage}/{name}/manuscripts` | `on_get, on_post` | `write:texts` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/texts/{genre}/{category}/{index}/chapters/{stage}/{name}/signs` | `on_get` | `public/guest` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/texts/{genre}/{category}/{index}/chapters/{stage}/{name}/unplaced_lines` | `on_get` | `public/guest` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/textsearch` | `on_get` | `public/guest` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/words` | `on_get` | `public/guest` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/words/all` | `on_get` | `public/guest` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/words/create-proper-noun` | `on_post` | `create:proper_nouns` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |
| `/words/{object_id}` | `on_get, on_post` | `write:words` | resource + schema in `ebl/*/web` and `ebl/*/application/*schema*.py` | DTO/schema dump in `ebl/*/web` + application schema | `200` for reads, `201/200` for writes where applicable | See `docs/standards/error-status-matrix.md` and domain-specific validation errors (`422/404/409`) | TODO: add concrete request/response payload examples |

## Completion Rules

1. Replace each TODO in Example Coverage with at least one valid request and one representative response payload.
2. For write endpoints, include one invalid payload example and expected `422` shape.
3. For scope-restricted endpoints, include one unauthorized scenario (`403`).
4. Keep this file updated whenever routes, schemas, or auth guards change.
